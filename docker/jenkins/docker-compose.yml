version: '3.8'

services:
  jenkins:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
      - ../../Build/generate.py:/build/generate.py

volumes:
  jenkins_home: